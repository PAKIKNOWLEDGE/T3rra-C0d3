{
  "name": "t3rra-c0d3",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "T3rra 桌面控制台（重写）：opencode 引擎 + ark 族界面。产品代码在 app/，调研与证据在 docs/ 与 traces/。",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "check:demo": "node tools/check-demo.mjs",
    "check:traces": "node tools/check-traces.mjs",
    "check:docs": "node tools/check-docs.mjs",
    "check:all": "npm run check && npm run test && npm run check:demo && npm run check:traces && npm run check:docs",
    "dev": "vite --config app/vite.config.ts"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
